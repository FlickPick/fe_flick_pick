<!DOCTYPE html>
<html lang ="en">
  <head>
    <h1 class="text-center text-bold pb-3 text-primary"><%= @details.movie_title %></h1><br>

    <div class="text-center">
      <img src= <%= @details.movie_poster %> alt= <%= @details.movie_title %>></br>
    </div>

    <div class="text-center text-bold pb-3 text-primary">
      <% if @details.cast %>
        <% @details.cast.each do |actor| %>
          <%= "#{actor&.[](:character)} is played by #{actor&.[](:name)}" %> </br>
        <% end %>
      <% end %>
    </div>

    <div class="text-center text-bold pb-3 text-primary">
      <%= link_to "See trailer on Youtube", @details.trailer %>
    </div><br>

    <div class="text-center">
      <iframe src='<%= @details.trailer %>?rel=0&autoplay=<%= params[:autoplay] || 0 %>' frameborder='0' allowfullscreen></iframe>
    </div>
  </head>
</html>