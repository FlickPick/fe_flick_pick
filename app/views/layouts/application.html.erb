<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body style="background-color: lightgray;">

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Flick Pick</a>
    <ul class="navbar-nav">
      <% if current_user %>
        <li class="nav-item">
          <a class="nav-link text-primary" href="/users/<%= current_user.id %>">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-primary" href="/sessions/<%= current_user.id %>" data-method="delete">Log out</a>
        </li>
      <% else %>
        <li class="nav-item">
          <a class="nav-link text-primary" href="<%= new_session_path %>">Sign in</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-primary" href="<%= new_user_path %>">Create User</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
<body>
  <%= yield %>
</body>
</html>

